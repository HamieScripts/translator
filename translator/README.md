# My Open Source GitHub Action

This is a simple GitHub Action that greets the provided name and returns the current time.

## Usage

```yaml
uses: your-username/my-action-name@v1
with:
  name: 'World'